import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    container:{
        height: '100%',
        width: '100%',
    },
    loader:{
        height: '80%',
        width: '100%',
        alignItems: 'center',
        justifyContent: 'center',
    },
})